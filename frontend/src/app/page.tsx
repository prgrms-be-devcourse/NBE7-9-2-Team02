export default function HomePage() {
  return <div>
    <h1>메인페이지 경로</h1>
    src/app/page.tsx
    <h2>
      src/app 에 있는 layout.tsx 에서 현재 const userStatus = 1;
      일 경우 마이페이지 링크가 보이고 1이 아닐 경우 로그인/회원가입 버튼이 보이도록 해두었습니다.
      프론트 구현하실 때는 실제 로그인 판별 로직이 필요합니다
      로그아웃도 해당 경로에 있습니다
    </h2>
  </div>;
}